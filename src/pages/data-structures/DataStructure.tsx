/*Tipos primitivos (string, number, boolean, null, undefined, symbol, bigint).
Estruturas de dados: Object, Array, Map, Set.
Variáveis e escopo: var, let, const.
Operadores lógicos, aritméticos, ternário.
Conversão de tipos (type coercion).
Diferença entre == e === */


export default function DataStructures() {
    
  return (
    <div>

    </div>
  );
}
